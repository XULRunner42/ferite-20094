// import statements

// declaration of expr(string[]) the recursive function

namespace Expr {
    function expr (array args) {
	return args;
	// return: single int number or a string with it
    }
}
